# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: smoke/smoke.login.spec.ts >> Smoke: Login >> Login page loads
- Location: tests/smoke/smoke.login.spec.ts:4:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator:  locator('#username')
Expected: visible
Received: undefined

```

# Test source

```ts
  1  | import { test, expect } from '../../src/fixtures/test.fixture';
  2  | 
  3  | test.describe('Smoke: Login', () => {
  4  |   test('Login page loads', async ({ loginPage }) => {
  5  |     await loginPage.goto();
> 6  |     await expect(loginPage.usernameInput).toBeVisible();
     |                                           ^ Error: expect(locator).toBeVisible() failed
  7  |     await expect(loginPage.passwordInput).toBeVisible();
  8  |     await expect(loginPage.locator('button[type="submit"]')).toBeVisible();
  9  |   });
  10 | 
  11 |   test('@smoke Login with valid credentials', async ({ loginPage, dashboardPage }) => {
  12 |     await loginPage.goto();
  13 |     await loginPage.login(process.env.TEST_USERNAME!, process.env.TEST_PASSWORD!);
  14 |     await expect(dashboardPage.welcomeMessage).toBeVisible();
  15 |   });
  16 | });
```